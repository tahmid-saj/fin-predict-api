{
  "builds": [
    {
      "src": "fin_predict_api/wsgi.py",
      "use": "@vercel/python",
      "config": { 
        "runtime": "python" 
      }
    }
  ],
  "routes": [
    {
        "src": "/(.*)",
        "dest": "fin_predict_api/wsgi.py"
    }
  ]
}